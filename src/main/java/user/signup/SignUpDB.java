package user.signup;

//database layer

//implements functions needed to query the database for users for signup use case.

public class SignUpDB implements SignUpDBGateway {
    @Override
    public boolean findOne() {
        return false;
    }
}
